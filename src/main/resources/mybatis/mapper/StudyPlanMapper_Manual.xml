<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.mapper.StudyPlanMapper_Manual">
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    _id, _content, _operate_date, _attachment_url
  </sql>
  <sql id="Example_Where_Clause">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <select id="selectListMap" parameterType="com.dao.beans.StudyPlanExample" resultType="java.util.HashMap">

    select
    <include refid="Base_Column_List"/>
    from _study_plan
    <if test="_parameter != null">
      <include refid="Example_Where_Clause"/>
    </if>
    order by _id

  </select>
  <insert id="batchInsert" parameterType="java.util.List">
    insert into _study_plan(_content, _operate_date, _attachment_url) values
    <foreach collection="list" item="item" index="index" separator=",">
      (#{item.content,jdbcType=VARCHAR},#{item.operateDate,jdbcType=DATE},#{item.attachmentUrl,jdbcType=VARCHAR})
    </foreach>
  </insert>
</mapper>